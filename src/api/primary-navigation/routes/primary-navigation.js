'use strict';

/**
 * primary-navigation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::primary-navigation.primary-navigation');
